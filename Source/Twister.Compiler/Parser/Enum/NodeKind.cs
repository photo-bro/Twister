namespace Twister.Compiler.Parser.Enum
{
    public enum NodeKind
    {
        Terminal = 0,
        Primitive,
        Statement,
        Expression,
        Function,
        Identifier,
        Symbol,
        Declaration
    }
}
